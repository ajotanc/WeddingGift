{
	"name": "cascade-delete",
	"version": "1.0.0",
	"description": "Appwrite function to cascade delete data when a user account is deleted",
	"main": "src/main.js",
	"type": "module",
	"scripts": {
		"format": "prettier --write ."
	},
	"dependencies": {
		"node-appwrite": "^20.2.1"
	},
	"devDependencies": {
		"prettier": "^3.2.5"
	}
}
