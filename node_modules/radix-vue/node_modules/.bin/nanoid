#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../nanoid/bin/nanoid.js" "$@"
else
  exec node  "$basedir/../../../nanoid/bin/nanoid.js" "$@"
fi
# cmd-shim-target=D:/Ajota/WeddingGift/node_modules/.pnpm/radix-vue@1.9.17_vue@3.4.21_typescript@5.9.3_/node_modules/nanoid/bin/nanoid.js
